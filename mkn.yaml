#! clean build -tSa "-O2 -fPIC -D_KUL_HTTPS_" -p test -l "-lssl -lcrypto -pthread" -d

name: httplus

parent: c

profile:
  - name: c
    inc: ./inc
    src: ./src
    dep: 
      - name: mkn.ram
        version: master
      - name: parse.yaml
        version: master


  - name: test
    parent: c
    inc: ./usr
    main: test.cpp
    mode: none
