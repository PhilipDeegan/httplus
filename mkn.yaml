

name: httplus

parent: c

profile:
  - name: c
    inc: ./inc
    dep: 
      - name: mkn.ram
        version: master
      - name: parse.yaml
        version: stable

  - name: test
    parent: c
    inc: ./usr
    main: test.cpp
    mode: none
